# AI 检测工具箱 更新日志

## 移除开发声明（2026-09-23）

> 本次**只改文案，未改动任何程序逻辑** —— 安装器与引导器仍是 v1.3.0。

- 移除项目文档与「关于」窗口中的「开发声明」段落，共 4 处：
  `README.md` / `README.en.md` / `app/core/about_text.py`（中文段 + 英文段）
- 该段落原文公开了作者使用的 AI 辅助工具链，属作者个人开发细节，不再对外展示
- 顺手修正英文免责声明中一处遗留断行（`The author is still a student;` 前次改动后单独成行）

## 仓库整理（2026-09-23）

> 本次**只调整仓库内容，未改动任何程序代码** —— 安装器与引导器仍是 v1.3.0，无需重新下载。

- 移除 5 个仅供作者本机使用的开发脚本（`tools/` 下的 `download_simpleai_model.py`、
  `run_detection_test.bat`、`make_icon.py`、`grab_screen.py`、`patch_offline_fix.py`）：
  它们写死了开发机的绝对路径，对使用者没有价值；模型与依赖的获取由首启引导器负责
- 移除 `docs/` 下 4 个与 `app/assets/` **逐字节重复**的图片，README 直接引用 `app/assets/`，
  省去一份冗余
- 保留 `tools/` 下的测试脚本：`test_engines.py`、`smoke_test.py`、`test_detect.py`、`fusion_selftest.py`
- 仓库回到「源码 + exe + 文档 + 测试」四类内容，无开发机痕迹

## v1.3.0（2026-09-19）

> 本版是**第一次把 v1.2.8 的「九引擎」真正打包发出去** —— v1.2.8 当时只存在于源码，
> 安装器 exe 还停在 v1.2.7。现在安装器与引导器都已重新打包，装完即是新版。

### 新增：全新应用图标
- 程序窗口、任务栏、安装器、首启引导器、桌面快捷方式、
  「设置 > 应用」卸载列表 —— 全部换成新图标（绿色圆角方块 + 放大镜 + 二进制文档）
- 多尺寸 `.ico`（16 / 24 / 32 / 48 / 64 / 128 / 256），高分屏与任务栏小图标都不糊
- **透明底、无白框**：圆角之外是透明的，浅色 / 深色主题下都不会出现"白底方块"；
  原图右下角的生成水印与投影已裁掉
- 任务栏归组修正：显式声明进程身份，窗口不再被算成 `pythonw.exe`（原先任务栏图标是 Python 的）

### 修复：全屏时按钮文字被裁（"花字"）
- 根因（实测）：左侧面板最小高度约 1082px，而 1440×900 屏幕上全屏只有 900px，
  Qt 把所有控件**等比压扁**，按钮从 34px 压到 21px，文字需 34px 就被裁掉。
  普通窗口时 Qt 偷偷把窗口撑到 1082（底部超出屏幕）所以看不出来
- 左面板改为**滚动**而不是压扁；按钮加最小高度锁定；
  新增统一的"窗口不超出屏幕"工具，主窗口 / 引擎管理 / 下载设置 / 降重 / 关于 全部套用
- 顺带修掉下载设置里「浏览…/删除」按钮被 `setFixedWidth(70/60)` 写死导致的裁字
- 回归：全部窗口被压扁的控件数 **19 → 0**（普通窗口与全屏都是 0）

### 含 v1.2.8 的全部内容
9 个学术方法全部落地为可运行模块，分「检查 / 修复 / 评测」三类，
模型一律按需下载、软件不预置，并预留插件与远端清单扩展接口 —— 详见下方 v1.2.8 条目。

## v1.2.8（2026-09-19）

### 新增：9 个引擎全部落地，分「检查 / 修复 / 评测」三类
以前 README / 关于里列的 9 项学术方法，只有 3 项真跑得起来，其余是纸面引用。
这一版全部做成可运行的东西：

**检查引擎（5 个，判断文本是否 AI 生成）**
- `SimpleAI / HC3`（默认中文引擎）—— 原有
- `GLTR` 困惑度 —— 原有
- `Fast-DetectGPT` —— **重写为论文的条件概率曲率**（采样近似），
  不再是"困惑度换个模型"的挂名实现
- `DetectGPT` —— **新增**，论文的掩码扰动实现：T5 补全随机挖掉的片段，
  比较对数概率曲率
- `Binoculars` —— **新增**，同词表双模型交叉困惑度比，免调阈值

**修复引擎（2 个，诊断 + 降重，内置规则不需要下载模型）**
- `aigc-reduce` 三轮降重协议、知网 5 种语言模式诊断 —— 原有，现在在引擎管理里可见可选

**评测基准（2 个，给检测器做体检）**
- `RAID`、`MGTBench` —— **新增可运行模块**：用带标注样本跑出准确率、
  假阳性率（人类文章被判成 AI 的比例）、假阴性率、精确率/召回率/F1，
  并按生成模型与语言分项；支持导入官方数据集子集（CSV / JSONL），
  可导出 Markdown 报告

### 新增：模型一律按需下载，软件不预置
和以前一样：清单里只声明"用哪个模型、多大、去哪下"，用户在「下载设置」
里点下载。默认中文场景只需 SimpleAI 一个（约 400MB）；
GLTR/Binoculars 约 0.5~1.9GB；Fast-DetectGPT 约 5.4GB。

### 新增：预留扩展接口（以后加模型不用重新打包）
1. **引擎插件目录** `engines_plugins/*.py` —— 丢一个文件进去，
   里面用 `@register("my_impl")` 声明，启动时自动发现；
2. **引擎清单可远端更新** —— 保存一份 `engines_manifest.json` 到任意可访问地址，
   在「引擎管理 → 检查更新」里填写 URL 即可拉到新引擎 / 新模型条目；
3. **模型由清单声明** —— 每个引擎用哪个 HuggingFace 模型写在清单的 `models` 里，
   换模型（比如把 Binoculars 换成 Falcon 组合）改清单即可，不必动代码。

### 界面
- 引擎管理重做：左侧按「检查 / 修复 / 评测」三类分组，右侧显示模型清单、
  体积、论文来源、本地是否已下载；内置规则引擎标注「内置可用」
- 检测引擎下拉只列检查类，不再混入修复 / 评测条目
- 新增「评测基准」入口（主界面 + 引擎管理）
- 下载设置里的模型列表改为按引擎展示，并显示下载状态

### 杂项
- 修正关于窗口里的失实描述：删掉"匿名遥测（可关闭）"（本项目没有遥测），
  技术架构段如实列出 5 个引擎与两类基准
- 新增回归测试 `tools/test_engines.py`（13 项：清单完整性、分类、清单覆盖、
  插件自动发现、模型解析、评测指标、样本导入、修复引擎可用性）

## v1.2.7（2026-09-19）

### 修复：安装失败的根因 —— 官方 Python 安装器的「MSI 孤儿注册」
- 安装器**不再调用官方 .exe 安装器**，改用官方 **embeddable 便携包**
  (`python-3.12.10-embed-amd64.zip`)：下载 → 解压到 `runtime\python` → 修补 `._pth`
  → 引导 pip。不写注册表、不需要管理员权限、卸载=删目录，
  彻底告别「静默安装退出码 0 却什么都没装上」以及「卸载/修复全 1603」
- 安装前自动清理不完整的旧运行时与历史遗留的 `venv`

### 修复：下载失败的根因 —— socks 系统代理
- 新增**网络自愈**（源码侧 `app/core/netfix.py` + 便携运行时里的 `sitecustomize.py`）：
  VPN / 加速器常把 Windows 系统代理写成 `socks://127.0.0.1:xx`，而 Python 的
  urllib / requests / pip 都不支持 socks —— 表现为 `BadStatusLine` 或
  `Missing dependencies for SOCKS support`，下载全挂（curl 却正常）。现在检测到这类
  代理会自动改走直连
- 下载链路升级为三段兜底：**urllib → 直连 urllib（绕开系统代理）→ 系统 curl**，
  并在下载后做体积 + 文件头（`PK`）双重校验，杜绝把错误页当安装包

### 修复
- 首次启动引导器改用**运行时 Python** 执行 pip（此前打包成 exe 后 `sys.executable`
  指向 exe 自身，pip 根本跑不起来）
- 桌面快捷方式 / 启动命令**直接指向 `first_run_gui.exe`**（此前用 pythonw 去"运行"
  一个 exe，等于把 exe 当脚本喂给解释器，必错）
- 引导器打包成 exe 却找不到 `runtime\python` 时给出明确提示，而不是继续硬跑

### 优化
- 安装逻辑全部抽成模块级函数，GUI 只是外壳；新增**无界面安装**：
  `AIGC_Toolkit_Setup.exe --cli <目标目录>`（便于批量部署与自测）
- 启动脚本、错误提示、手动选包对话框同步改为便携包说法

---

## v1.2.6（2026-09-19）

### 修复
- **HiDPI 缩放下的文字重影 / 花字**：去掉半透明窗口与阴影效果（分数缩放下会渲染错位），
  显式指定「微软雅黑 UI」字体

### 优化
- 按设计规范重做界面：发丝边框、单一强调色（#2563eb 占比 <5%）、中性灰底、
  圆角 ≤16px、细进度条，替换原玻璃拟态渐变风格

---

## v1.2.5（2026-09-18）

### 修复
- **打开闪退**：`torch` 被安全软件（实测为搜狗浏览器驱动 `SCAegis64.sys`）拦截加载时，
  改为容错启动并给出友好提示，不再直接崩溃

---

## v1.2.4（2026-09-18）

### 新增
- **控制面板卸载入口**：安装后可在 Windows「设置 > 应用」或控制面板「卸载程序」中卸载本软件
  - 卸载前会列出将删除的目录，并提示遇到 Bug 可先邮件反馈（gxgx3456@qq.com）
  - 卸载时自动清理注册表卸载键、桌面快捷方式与整个安装目录（含模型文件）
- **安装选项可勾选**（均默认开启）：
  - 创建桌面快捷方式
  - 安装完成后立即运行（自动拉起首次启动引导）
- 安装器**全屏按钮移至右上角**（系统按钮风格，Esc 退出）

### 优化
- 「关于」页面补充 v1.2.3 / v1.2.4 更新说明（中英双语）

---

## v1.2.3（2026-09-18）

### 新增
- **自动降重闭环**（此前为半自动，只改一轮）：
  - 降重窗口新增「自动降重（循环复检）」按钮
  - 每轮：规则改写 → 本地模型复检全文 AI 率 → 仍超标则只对超标段落再改
  - 循环直到 AI 率达到目标值（可设，默认 ≤25%）或最大轮数（可设，默认 3，防无限循环）
  - 完成后弹窗汇报每轮改写段数、复检 AI 率与最终 AI 率，段落清单/诊断同步刷新

### 优化（安装器下载方案，解决部分用户下载失败）
- 下载源扩为 **npmmirror → 华为云 → python.org** 多镜像依次尝试（国内镜像无需 VPN）
- 每个源：Python 自带下载失败自动切换**系统 curl** 兜底（独立网络栈）
- 下载完成后体积校验（<5MB 判为坏包，自动换源重下）
- 所有源均失败时，可**手动选择本地已下载的安装包**，完全不依赖网络

---

## v1.2.2（2026-09-13）

### 修复
- Python 静默安装「退出码 0 却失败」：官方安装器被旧注册表记录干扰时，
  改为从注册表/默认位置找回真实安装目录并复制；仍失败则用可见模式重试一次
- 安装器新增**左上角全屏按钮**（查看长日志更方便，Esc 退出）
- 首次启动引导器增加文件日志（`app/logs/first_run.log`）与主程序 stderr 捕获

---

## v1.2.1 / v1.2.0（2026-09 上旬）

- **轻量安装器**：AI 组件（PyTorch/模型）改为首次启动时自动下载，CPU/CUDA 自动选择
- **模型保存路径可自定义**（可放 D 盘），检测/降重引擎统一按配置解析模型目录
- 首次启动引导器 `first_run.py`：进度条下载 + 完成后自动启动主程序
- 内置国内镜像加速（HF 镜像 / 清华 PyPI / npmmirror）
- 模型路径、降重目标等设置项支持随预设存档

---

## 反馈

遇到 Bug 或有建议，请邮件联系：**gxgx3456@qq.com**
